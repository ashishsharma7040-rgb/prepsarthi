{
  "name": "prepsarthi-functions",
  "description": "PrepSarthi Firebase Cloud Functions",
  "scripts": {
    "build": "echo 'No build step — using CommonJS'",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0",
    "googleapis": "^140.0.0"
  },
  "devDependencies": {},
  "private": true
}
